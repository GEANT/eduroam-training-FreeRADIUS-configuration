#
#   eapol_test -c ttls-pap.conf -s testing123
#
network={
        ssid="example"
        key_mgmt=WPA-EAP
        eap=TTLS
        identity="user@real.realm.si"
        anonymous_identity="anonymous@real.realm.si"
        password="eduroam"
        phase2="auth=PAP"

	#
	#  Uncomment the following to perform server certificate validation.
#	ca_cert="/etc/raddb/certs/ca.der"
}

